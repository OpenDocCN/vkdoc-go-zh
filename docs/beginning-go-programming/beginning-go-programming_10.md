# 索引

## A

- 应用程序编程接口 (API)
- 数组：访问、声明、索引编号、初始化、内存位置、查询、大小、用途
- 赋值运算符
- 原子计数器：`atomic` 包

## B

- `bestBid()` 函数
- `bidOn()` 函数
- 按位运算符

## C

- 不区分大小写的比较
- 代码组织
- 编译器优化
- 并发：Go 协程（参见 协程）
- 持续集成 (CI)
- `creditToBalance()` 函数
- 自定义构造函数

## D

- 日期和时间：类型，`dateWorker()` 函数
- `debitFromBalance()` 函数
- 依赖包管理

## E

- 优雅常量（参见 IOTA）
- 空接口
- 错误处理机制
- Go 中的显式转换，数值类型

## F

- `filter` 函数
- 函数：任意数量的值、自定义函数、声明、定义、形式参数、`func` 关键字、函数调用、`len()` 函数、`main` 函数、参数、传递参数（按引用调用、按值调用）、返回多个值、`return_type`

## G

- 垃圾回收器 (GC)
- 泛型
- Git 的 SHA，构建 ID
- `gofmt`，`goimports`
- Go 编程：前身、出现次数、执行、特性、目标、Go Playground、Hello World!、IDE、安装、JSON 数据、关键字、操作系统和 IO 包、包、组成部分、静态类型、语法规则（内置函数、大小写敏感性、代码块、分号）
- Go 配方：`defer`/`panic` 恢复、映射、数字、切片
- `gorilla/mux` 包
- 协程：优势、通道、复杂性与脆弱性、并发、并发代码、转换、数据格式（CSV 文件）、`downloadsSize()` 函数、错误组、Go 程序、HTTP 服务器代码、轻量级线程、主要配方、顺序代码、共享资源（原子计数器、并发操作影响、互斥、同步）（参见 同步协程）
- `grep()` 函数

## H

- `handleGet()` 函数
- `handleList()` 函数
- `handleSet()` 函数
- `http.Get()` 函数
- 超文本传输协议 (HTTP)：API、身份验证、Go 中的调用、客户端代码、`get()` 函数、`handleGet()` 函数、`handleList()` 函数、`handleSet()` 函数、`httpbin.org`、`http` 包、`list()` 函数、`main()`、使用 `gorilla/mux` 的 REST、服务器端代码、`set()` 函数、结构、编写

## I

- 幂等性
- 隐式转换
- 集成开发环境 (IDE)
- 接口：声明、定义、Go 配方、实现
- IOTA：自动递增、自定义类型、定义、表达式、泛型、整数值、JSON 编码/解码、将恰当名称赋予概念、跳过值、棘手的常量
- `isOdd` 函数

## J, K

- JavaScript
- JavaScript 对象表示法 (JSON)：自定义类型、函数、Go 类型、`mapstructure`、编组、方法、缺失值、包、解析、Go 配方、JSON 响应、`laggingStations()` 函数、解组、零值

## L

- 逻辑运算符

## M, N

- 主协程
- 映射：声明、添加条目、删除、哈希表、迭代（显示项、`for` 循环、键和值变量、`range`）、`sort()` 函数、键、`make()` 函数、用途
- 数学运算符
- 数学包
- 内存：内存管理、`make` 函数、内存分配、内存释放、`new` 函数
- 方法：声明、狗对象、Go 配方、接收者参数、接收者类型、返回值、用途
- `monthDistance()` 函数
- `multiMedian()` 函数
- `mutex`
- 互斥：`mux.Router`

## O

- 面向对象编程语言
- 运算符优先级

## P, Q

- 包：`go list` 工具/模板、导入、模块化、组织代码
- 绘图程序
- `Parse()` 函数
- 指针：`*` 与 C 风格语言的对比、声明、定义、协程与 Java 的对比、用途
- `poolWorker()` 函数
- 谓词
- 程序流程：`for` 循环（空置的初始化/后置语句、无限循环、步进变量/条件/范围、用途、`while` 循环）、`goto` 语句、`if` 语句、`switch` 语句
- 编程语言：编译型、静态类型

## R

- 实时竞价 (RTB)
- 实际应用
- 引用值（参见 内存管理）
- 正则表达式：`FindStringSubmatch()` 函数、`MustCompile()` 函数、`parseLine()` 函数、解析、`regexp.MatchString()` 函数、`ReplaceAllString()` 函数
- 关系运算符

## S

- 顺序代码
- `Signature()` 函数
- 切片：`append()` 函数、`cap()` 函数、`copy()` 函数、声明、定义、`len()` 函数、`nil`、`sort()` 函数、子切片
- 字符串格式化
- 结构体：访问成员、声明、Go 配方、实例、将 Person 指针传递给结构体、类型和结构体语句、用途、变量
- `sync/atomic` 包
- 同步协程：Go 配方、`errgroup`、协程池化、多协程、协程池化、`sync.Once`、`sync.WaitGroup`、超时

## T

- 文本文件：解析、读取、写入
- 超时
- 类型嵌入

## U

- Unicode
- 用户输入：`bufio` 包、函数、`Scanf` 函数、`Scanln` 函数

## V, W, X, Y, Z

- 变量：数据类型、声明（显式、隐式）、命名约定
