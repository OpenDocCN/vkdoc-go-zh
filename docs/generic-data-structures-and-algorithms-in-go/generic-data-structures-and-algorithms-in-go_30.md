# 索引

## A

抽象数据类型 (ADTs)  
- `game`: `game of life`的控制台实现、GUI 实现  
- `Go`: `counter` 包、创建计数器包、机制  
- `OOP`: 应用、多态、使用组合、使用类  

`go.mod` 文件  

`grid` 程序输出  

`stacks`

`Adelson-Velsky 和 Landis (AVL) 树`:
- `avl` 包代码实现  
- `deleteNode` 函数  
- `IntelliJ IDEA` 主驱动程序代码  
- `map`  
- `rightRotate(node)` 函数  
- `rotateDelete` 函数  
- `Search` 方法  
- 二叉搜索树  
- 比较  
- 集合构造  
- `concurrentAVLSet`  
- `dataSet` 切片  
- 删除  
- `floatset` 包  
- `insert` 和 `delete`  
- 插入  
- 有趣的事实  
- 树旋转  

`算法效率`:  
- `Big O` 搜索  
- 数组切片  
- 数字切片，确定  
- 排序 (参见 `排序算法`)  
- 速度效率，使用并发描述  

`人工智能 (AI)`

## B

`Big O`  
`二分查找`  
`二叉搜索树 (BST)`:
- 删除  
- 泛型实现  
- 数据结构  
- 删除  
- 图形化  
- `InOrderTraversal`  
- `insert`  
- `主驱动程序`  
- 方法  
- 包  
- 支持函数  
- `type OrderedStringer`  
- `inorder` 遍历  
- 插入  
- 概述  
- 搜索  
- `String()` 函数  

`二叉树`:  
- 绘制树  
- 树遍历  

`二十一点`，简化版游戏  

`自底向上动态规划`  
`分支定界算法`  
`暴力计算`:
- 寻找排列  
- `旅行商问题`  

`冒泡排序算法`

## C

`洗牌模型`  
`通道`  
`类`，使用抽象数据类型  
`组合优化问题`  
`并发性`:
- 通道  
- 通道方向  
- 使用 goroutine 生成质数  
- goroutines, 使用 goroutines 生成 `斐波那契数列`, 使用 goroutines 下棋  
- `互斥锁`  
- 竞态条件  
- `分段筛算法`  
- `select` 语句  
- `埃拉托斯特尼筛法算法`  
- 筛法解决方案  
- 使用 `quit` 通道  
- `WaitGroup`  

`转换十进制数，到二进制`

## D

`双端队列`  
`迪杰斯特拉算法`  
`DNA 子序列`  
`双向链表`  
`绘制树`:
- 二叉树结构  
- 代码解释  
- `go.mod` 文件，位于子目录 `binarytree` 和 `main` 中  
- 基础设施  
- `ShowTreeGraph`，实现  

`动态规划`:
- `DNA 子序列`  
- `背包问题`  
- `第 n 个斐波那契数`

## E

`生态模拟`:
- 代码实现  
- 数据模型设计  
- `findRandomCritter` 方法  
- `鲭鱼`  
- `Move` 方法  
- 输出  
- 概述  
- `reflect.TypeOf` 方法  
- `鲨鱼`  
- 支持函数  
- `金枪鱼`

`表达式树`:
- 构建新树  
- 构造  
- 解释  
- 函数求值  
- 数学表达式  
- `ShowTreeGraph` 函数

## F

`FeedForward` 函数  
`斐波那契数列`，使用 goroutines  
`过滤函数`

## G

`游戏`:
- 控制台实现  
- 网格细胞演化，规则  

`泛型`:
- 优点  
- 参数  

`泛型集合`，哈希表  
`遗传算法`:
- 定义  
- 实现  
- 精英群体  
- 形成下一代  
- 初始种群，随机路径  
- 父代配对  
- 锦标赛选择  
- `TSP` 配对过程  
- `PopSize` 步骤  

`归零` 函数  
`Go`:
- 基准测试  
- 并发应用  
- 并发性  
- 历史与描述  
- 中的抽象数据类型  
- `ADT 计数器`  
- `ADT`，实现  
- `counter` 包，创建计数器包，机制  
- `OOP` 应用  
- 使用组合的多态  

`gofmt` 工具  
`Go`，泛型参数:
- 约束泛型类型  
- 过滤函数  
- 泛型  
- 泛型，优点  
- 泛型类型，实例化  
- 接口，实现  
- `使 MyFilter 泛型化`  
- `使 MyMap 泛型化`  
- `Map` 函数  
- 按 ID 号添加新生  
- 按姓名添加新生  
- 按 `Student` 结构体添加新生  
- `sort` 包  
- `sort` 类型  
- `stringer` 类型  
- 无约束泛型类型 `any`  

`go.mod` 文件  
`协程`:
- 使用协程生成斐波那契数  
- 使用协程下棋  

`图`:
- `AddEdge` 方法  
- `AddVertex` 方法  
- 广度优先搜索方法  
- 定义与遍历  
- 深度优先搜索方法  
- `Dijkstra` 算法  
- 有向图  
- `Kruskal` 算法，代码实现  
- 最小生成树  
- `NewGraph` 函数  
- `OrderedStringer` 接口  
- 队列  
- 遍历算法  
- 元组  
- 访问变量  
- 加权图  

`网格`，ADT  
`GUI`，实现，生命游戏

## H

`哈希加密`  
`哈希表`:
- 构建  
- 冲突与冲突解决  
- 创建  
- 确定  
- 泛型集合  
- `哈希加密`  
- 插入  
- 负载因子  
- `map`  
- 性能  
- `Rabin-Karp 算法`  
- 滚动哈希计算  
- 字符串搜索  

`堆排序`  
`堆树`:
- 应用  
- 构造  
- 删除  
- 堆排序  
- 构建逻辑  
- 包  
- 包 `heap`，解释  

`霍纳法则`

## I, J

`中序遍历`  
`迭代器`

## K

`背包问题`:
- 暴力解法  
- 动态规划解法  

`Kruskal 算法`

## L

`线性查找`  
`链表`:
- 双向链表  
- 单向链表  

## M

`机器学习`  
`Map`  
`Map 函数`  
`迷宫应用`:
- 构建  
- 基础设施  
- 完成  
- 有效策略  

`归并排序算法`  
`Move 方法`  
`互斥锁`  
`MyFilter 泛型`

## N

`神经网络`:
- 具体例子  
- 定义  
- 实现  
- 层  
- 矩阵  
- 神经元  
- 输出  
- 成本的偏导数，权重  
- 感知器  
- 训练  

`节点实现`，栈  
`节点队列实现`  
`性能`  
`第 n 个斐波那契数`:
- 自底向上动态规划  
- 递归解法  
- 自顶向下动态规划

## O

`面向对象编程 (OOP) 应用`  
`OOP 应用`，单词的排列群组

## P

`单词的排列群组`  
`泊松过程`，队列应用  
`多态`  
`后缀求值`，栈  
`后缀表达式`，求值  
`后序遍历`  
`前序遍历`  
`优先队列 (PQ)`:
- 实现  
- `TSP`  

`程序输出`

## Q

`队列`:
- ADT 应用  
- 迭代器  

`快速排序算法`:
- 冒泡排序  
- 并发快速排序  
- 最坏情况

## R

`Rabin-Karp 算法`  
`递归解法`，第 n 个斐波那契数  
`红黑树`:
- 定义  
- 示例  
- 实现  
- 插入过程  
- 插入操作，详细步骤说明  
- 性能  

`滚动哈希计算`，哈希表

## S

`搜索`:
- 数组切片  
- 二分查找  
- 并发搜索  
- 线性查找  

`分段筛算法`  
`洗牌`，洗牌模型  
`埃拉托斯特尼筛法算法`  
`Sigmoid 激活函数`  
`模拟退火`，TSP:
- 算法  
- 人工温度变量  
- 组合优化  
- 收敛问题  
- 定义  
- 启发式解法  
- 实现代码  
- 线交叉操作  
- 结果  
- 状态  

`模拟逻辑`，队列应用  
`单向链表`  
`切片队列实现`  
`性能`  
`Smalltalk` 语言  
`排序算法`:
- `Big O` 分析  
- `冒泡排序` 算法  
- `并发归并排序`  
- `并发快速排序`  
- `归并排序` 算法  
- `快速排序` 算法  
- `快速排序`，`冒泡排序` 和 `快速排序` 的最坏情况  

`栈`:
- ADT  
- 转换十进制数  
- 效率  
- 节点和切片  
- 函数求值  
- `归零` 函数  
- `迷宫应用`  
- 节点实现  
- 后缀求值  
- 后缀表达式，求值  
- 切片实现 `T`，声明为有序类型  

`语句`，并发性  
`稳态模式`  
`Stringer`  
`字符串搜索`，哈希表

## T, U, V

`自顶向下动态规划`  
`旅行商问题 (TSP)`:
- 分支定界算法  
- 用于该问题的分支定界  
- 分支定界实现  
- 分支定界解法，生成  
- 暴力计算  
- 精确暴力解法  
- 五城市示例，逐步说明与历史  
- 下界，计算  
- 优先队列  
- 优先队列，实现  
- 路径，展示  

`树遍历`:
- 中序遍历  
- 后序遍历  
- 前序遍历  

`trueOutput` 函数

## W, X, Y, Z

`WaitGroup`  
`等待队列`，离散事件模拟
